<?php 


$text = "Monirul Middya";

// The PHP strlen() function returns the length of a string.
$len = strlen($text);

// echo $len;

// The PHP str_word_count() function counts the number of words in a string.

// echo str_word_count("The PHP strpos() function searches for a specific text within a string. If a match is found, the function returns the character position of the first match. If no match is found, it will return FALSE.");


// Search for the text "world" in the string "Hello world!":

// strpos(string , match_word);

$artical = "The PHP strpos() function searches for a specific text 300 within a string. If a match is found, the function returns the character position of the first match. If no match is found, it will return FALSE.";

// echo strpos($artical,"musta123");

// if(strpos($artical,"100")){
//     echo "Rs 100";
// }elseif(strpos($artical,"200")){
//     echo "Rs 200";
// }else{
//     echo "Not match";
// }



// The PHP str_replace() function replaces some characters with some other characters in a string.

// echo str_replace("Khan", "Middya", "Najibul Khan");


?>